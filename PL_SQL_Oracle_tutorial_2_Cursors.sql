/*
PL/SQL Oracle tutorial - Cursors - PL/SQL basics (Lesson 2)

A Cursor is a named SQL SELECT statement that we can use in our PL\SQL programm to access multiple rows from a table and 
retrive them one row ata time.

A cursor is a mechanism by which we can assign a name  to a "select statement" and manipulate the information 
within that SQL statement.

There are two types of cursors: implicit and explicit.

Курсор - это именованный оператор SQL SELECT, который мы можем использовать в нашей программе PL \ SQL для доступа к нескольким 
строкам из таблицы и извлекать их по одной строке за раз.

Курсор - это механизм, с помощью которого мы можем присвоить имя «оператору SELECT» и управлять информацией.
внутри этого оператора SQL.

Есть два типа курсоров: неявные и явные.
*/